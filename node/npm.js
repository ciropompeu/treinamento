// módulos

// um bloco de códigosl reutilizável ou uma unidade de funcionalidades
// cuja existência não altera incidentalmente o comportalmento de
// outros blocos do código

// bloco de códigos em arquivos, que resolvem problemas específicos, que não impactam o comportamento de outros módulos do sistema

//http:// node.org/api

// app.js é o entry point (ponto de entrada) que o node usará para começar  a executar a aplicação

// require() - segunda etapa, solicitar o módulo que desejo dentro do arquivo de entrada
// o módulo file system chama
// const fs = require("fs");

// ao enviar um módulo com o require um objeto literal é gerado na variável
// que terá muitas variáveis e metodos disponíveis para uso


